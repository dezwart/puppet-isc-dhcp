
# Host fragment for <%= name %>

host <%= name %> {
  hardware ethernet <%= mac %>;
  fixed-address <%= name %>;
  host-name <% hostname=name.split("\.")[0] %><%= hostname %>;
<% if @routers %><% first = true %> option routers <% routers.each do |router| -%><% if first != true %>, <%= router %><% else %><%= router %><% first = false %><% end %><% end %><% end %>
<% if @filename %>  filename "<%= filename %>";<% end %>
<% if @next_server %>  next-server <%= next_server %>;<% end %>
}
